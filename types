export interface VisualContext {
  title: string;
  explanation: string;
  imageUrl: string;
  thumbnailUrl?: string;
  searchQuery: string;
  imageSource: "serpapi" | "fallback" | "none";
  active: boolean;
}
